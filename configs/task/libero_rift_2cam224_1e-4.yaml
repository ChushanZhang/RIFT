# @package _global_

defaults:
  - override /data: libero_2cam
  - override /model: rift
  - _self_

batch_size: 16
num_workers: 8

lr_scheduler_type: cosine
learning_rate: 1.0e-4
num_epochs: 10
max_steps: null
log_every: 10
save_every: 2000

gradient_accumulation_steps: 1
weight_decay: 1.0e-2
resume: null

wandb:
  project: rift
  name: libero_rift_2cam224_1e-4
